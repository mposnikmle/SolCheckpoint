import React, { FC } from 'react';

interface BalanceProps {
  // Define the type for your props here
}

const Balance: FC<BalanceProps> = (props) => {
  return (
    <>
      <h1>Hello from Balance</h1>
    </>
  );
};

export default Balance;